function power_dual_lower_bound(A, b, S, w, z_hat)
    # TODO
end

function diagonal_design_lower_bound(A, b, S, w, z_hat)
    # TODO
end

function special_lower_bound(A, b, S, w, z_hat; list=[])
    # TODO
end